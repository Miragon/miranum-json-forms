# Miranum Json Forms

## Project setup
```shell
npm install
```

### Build the VS Code Extension
```shell
npm run build
```

### Open VS Code
```shell
code .
```

Use `F5` to open the `Extension Host`
